# ==============================================
# PAYMENT SERVICE - LOCAL PROFILE
# ==============================================
# This profile uses H2 in-memory database and
# disables external dependencies for local testing
# ==============================================

server:
  port: 8082

spring:
  # Disable Config Server
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
  config:
    import: "optional:"
  
  # Exclude Redisson auto-configuration
  autoconfigure:
    exclude:
      - org.redisson.spring.starter.RedissonAutoConfigurationV2
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  
  # H2 Database
  datasource:
    url: jdbc:h2:mem:payment_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  # H2 Console
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  # Disable Flyway
  flyway:
    enabled: false
  
  # Disable Redis
  data:
    redis:
      repositories:
        enabled: false
  
  # Kafka disabled
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false
  
  # Security
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081

# JWT Configuration (same secret as auth-service)
jwt:
  secret: local-256-bit-secret-key-for-signing-jwt-tokens-must-be-at-least-32-characters-long

# Disable Redisson
redisson:
  config: ""

# Eureka disabled
eureka:
  client:
    enabled: false

# Gateway Configuration
# Using Razorpay for real payment testing
app:
  gateway:
    provider: RAZORPAY  # Using Razorpay for Indian payments

# ===========================================
# PAYMENT GATEWAY CONFIGURATIONS (Local)
# ===========================================

# Razorpay Configuration (Recommended for India)
razorpay:
  key-id: rzp_test_SCtKfVntrKGE4L
  key-secret: Ln9cedR0uvlfrQXXYUFnmMSI

# Stripe Configuration (for international testing)
# Replace with your Stripe TEST mode keys from https://dashboard.stripe.com/test/apikeys
stripe:
  api-key: ${STRIPE_API_KEY:sk_test_placeholder}
  webhook-secret: ${STRIPE_WEBHOOK_SECRET:whsec_placeholder}

# Logging
logging:
  level:
    root: INFO
    com.foodexpress.payment: DEBUG
    com.razorpay: DEBUG
    com.stripe: DEBUG
    org.hibernate.SQL: DEBUG
